export type Team = {
    id: string;
    name: string;
    description: string;
    imageUrl: string;
  };

  
  
    export type Player = {
        id: string;
        name: string;
        position: string;
        imageUrl: string;
    };